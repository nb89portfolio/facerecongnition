import React from 'react';
import './signin.css';
import Logo from '../logo/Logo';

const Signin = () => {
    return(
        <nav>
            <Logo />
            <p>Sign Out</p>
        </nav>
    );
}

export default Signin;